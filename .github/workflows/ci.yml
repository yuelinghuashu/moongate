name: Deploy to Aliyun

on: push

jobs:
  ci:
    runs-on: ${{ matrix.os }}

    strategy:
      matrix:
        os: [ubuntu-latest]
        node: [22]

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Install pnpm
        uses: pnpm/action-setup@v4

      - name: Install node
        uses: actions/setup-node@v6
        with:
          node-version: ${{ matrix.node }}
          cache: pnpm

      - name: Install dependencies
        run: pnpm install

      - name: Lint
        run: pnpm run lint

      - name: Build
        run: pnpm run build

      - name: 🌐 网络连通性终极测试
        run: |
          echo "测试连接到 ${{ secrets.SERVER_HOST }}:22 ..."
          # 使用 telnet 风格测试，这是最原始的网络测试
          timeout 3 bash -c "(echo > /dev/tcp/${{ secrets.SERVER_HOST }}/22) 2>/dev/null" && 
            echo "✅ 成功：服务器的22端口可从GitHub Actions网络访问" || 
            echo "❌ 失败：无法连接到服务器的22端口。根本原因：阿里云安全组或服务器防火墙阻止。"

      - name: Deploy to Server via Rsync
        uses: burnett01/rsync-deployments@7.0.1
        with:
          switches: -avz --delete # 递归、压缩、同步删除
          path: .output/public/ # 👈 Nuxt 的静态文件输出目录
          remote_path: /var/www/my-site/ # 👈 服务器上的目标目录
          remote_host: ${{ secrets.SERVER_HOST }} # 服务器 IP
          remote_user: ${{ secrets.SERVER_USER }} # 登录用户名
          remote_key: ${{ secrets.SSH_PRIVATE_KEY }} # SSH 私钥
